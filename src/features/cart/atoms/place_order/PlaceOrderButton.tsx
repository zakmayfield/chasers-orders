import { Btn } from '@/shared/components/ui';

type TPlaceOrderButtonProps = {};

export const PlaceOrderButton = (props: TPlaceOrderButtonProps) => {
  return <Btn text='Place Order' bgColor='green' width='full' />;
};
