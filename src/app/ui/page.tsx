import { Headings } from './components';

export default async function Page() {
  return (
    <div className='border w-3/4 mx-auto flex flex-col gap-12'>
      {/* Headings */}
      <Headings />
    </div>
  );
}
